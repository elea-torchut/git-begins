cloner un repository
	git clone <URL>
3 commandes de push*
	git add <nom du fichier>
	git commit -m "commentaire"
	git push (git push origin main)
commande pull
	git pull <nom_du_dépôt_remote> <branche_distante>
